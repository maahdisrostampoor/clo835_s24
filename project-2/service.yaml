apiVersion: v1
kind: Service
metadata:
  name: time-service-service
spec:
  type: NodePort
  selector:
    app: time-service
  ports:
    - protocol: TCP
      port: 3030
      targetPort: 3030
      nodePort: 30000  # NodePort can be any available port in the range 30000-32767

